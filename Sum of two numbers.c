#include<stdio.h>
void main(){
    int num1 , num2 , sum;//Declaring the datatype and the variables for the two numbers and their sum
    num1 = 10; //Giving the value of first number right now . In later codes we will take the number as input from the user
    num2 = 5;
    sum = num1 + num2;//Calculating the sum of the two numbers 
    printf("The sum of %d and %d is : %d\n" , num1 , num2 , sum);//printing the output
}